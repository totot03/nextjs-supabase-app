{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | tr -d '\\r' | { read -r f; case \"$f\" in *.ts|*.tsx|*.js|*.jsx) npx eslint --fix \"$f\" 2>/dev/null || true; npx prettier --write \"$f\" 2>/dev/null || true;; esac; }",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
